# Project Overview

このリポジトリは、ゲーム「ダダサバイバー（Survivor.io）」のゲーム内イベント日程と更新状況を表示する非公式の静的Webアプリです。ゲーム内日付は UTC+8 を基準とし、カレンダー、選択日のイベント情報、残り日数、更新日、ギルド向けコピー文を提供します。表示言語は日本語・英語・中国語です。

変更前に必ず関連する既存実装、設定、ドキュメント、GitHub Actionsを調査してください。既存機能と既存データの意味を維持し、不明点を推測して仕様を変更しないでください。仕様判断に必要な情報が不足している場合は、実装前にユーザーへ確認してください。

# Tech Stack

- フロントエンド: HTML5、CSS、Vanilla JavaScript
- データ・設定: JSON
- 日次データ生成: Node.js（CommonJS、Node.js標準ライブラリのみ）
- ホスティング: GitHub Pages
- 自動化: GitHub Actions
- 外部アセット: Google Fonts（Noto Sans JP）

フレームワーク、バンドラー、データベース、アプリケーションサーバー、npm依存パッケージは現在使用していません。依頼がない限り、新しいフレームワークや重量級依存関係を導入しないでください。

# Architecture

- `index.html`: Webアプリのエントリーポイントと画面構造
- `style.css`: レスポンシブレイアウト、色、状態表示、モーダルなどのスタイル
- `app.js`: ブラウザ側のアプリケーションロジック
  - `TimeManager`: UTC+8基準の日付計算
  - `DataManager`: `config.json`、翻訳、`localStorage`の管理
  - `EventCalculator`: イベント周期と状態の計算
  - `UIManager`: DOM、カレンダー、イベント一覧、設定画面、コピー機能
- `config.json`: イベント定義、翻訳、アプリ設定の正本
- `export-events.js`: `config.json`から当日のイベント状態を計算するNode.js CLI
- `daily_events.json`: `export-events.js`が生成する日次データ
- `.github/workflows/daily_update.yml`: 日次データを生成してコミットするGitHub Actions
- `README.md`: 利用方法とローカル起動方法
- `github_setup.md`: GitHub Pagesの公開手順
- `robots.txt`、`sitemap.xml`: 公開サイトのクローラー向け設定

バックエンド、独自HTTP API、認証基盤、データベースはありません。ブラウザは同一サイト上の`config.json`を取得し、計算と描画をクライアント側で行います。

# Development

HTMLを直接`file://`で開くと`config.json`の取得がブラウザ制約で失敗するため、リポジトリルートで静的HTTPサーバーを起動してください。

```sh
python -m http.server
```

既定の確認先は `http://localhost:8000/` です。VS Code Live Serverまたは`npx http-server`も利用できますが、既存プロジェクトにはnpm依存関係がないことに留意してください。

日次JSONを生成するコマンドは以下です。

```sh
node export-events.js
```

このコマンドは`daily_events.json`を上書きします。調査や検証だけが目的の場合は、意図せず生成物を変更しないようにしてください。

# Build

現在、ビルド工程はありません。`index.html`、`style.css`、`app.js`、`config.json`をそのままGitHub Pagesで配信します。

ビルドツールを導入する場合は、依頼された範囲に必要かを先に確認し、GitHub Pagesのルート配信、静的ファイルの相対パス、既存のローカル起動方法を壊さないでください。

# Test

現在、自動テスト、lint、formatter、型検査は設定されていません。これは検証を省略してよいという意味ではありません。

最低限、変更内容に応じて以下を実行してください。

```sh
node --check app.js
node --check export-events.js
```

`config.json`を変更した場合はJSONとして解析できること、必須キー、イベントIDの一意性、参照する翻訳キー、日付・周期設定を確認してください。イベント計算を変更した場合は、開始前、開始日、更新日、最終日、周期切替日、UTC+8の日付境界を確認してください。

テストを追加する場合は、ブラウザ版とNode版のイベント計算結果が一致することを優先して検証してください。変更後は、関連するテストまたは構文・ビルド相当の検証を必ず実行し、実行できない場合は理由を報告してください。

# Coding Rules

- 既存コードは4スペースインデント、セミコロン付き、シングルクォート中心のJavaScriptです。周辺コードの形式に合わせてください。
- クラス名は`PascalCase`、関数・メソッド・変数は`camelCase`、定数は`UPPER_SNAKE_CASE`を使用してください。
- イベントID、設定キー、翻訳キーは既存の`snake_case`およびドット区切り形式を維持してください。
- 日付キーは`YYYY-MM-DD`形式を使用してください。
- ゲーム内日付はUTC+8基準です。JSTでは午前1時にゲーム日付が変わる仕様を維持してください。
- 日数計算では時刻やローカルDSTの影響を避け、既存の`TimeManager`の方針を踏襲してください。
- 新しいイベント種別や設定項目を追加する前に、既存の6種別で表現できないか確認してください。
- ブラウザ版`app.js`とNode版`export-events.js`に重複する計算ロジックを変更する場合は、両方の挙動を必ず確認してください。一方だけを更新して結果を不一致にしないでください。
- コメントは「何をしているか」より、UTC+8、周期起点、ゲーム固有例外など「なぜ必要か」を説明してください。
- 依頼されていない大規模リファクタリング、全体的な再フォーマット、ファイル移動、技術スタックの置換を行わないでください。
- 変更は目的に必要な最小範囲に限定し、既存機能を壊さないでください。

# UI / UX Rules

- 既存のシンプルなカレンダー中心のデザインと状態別カラー表現を維持してください。
- デスクトップでは2か月、モバイルでは主対象の1か月を表示するレスポンシブ仕様を維持してください。
- 主要操作はカレンダー移動、日付選択、今日へ戻る、設定、コピーです。変更後もキーボード操作、ボタンのラベル、基本的なアクセシビリティを損なわないでください。
- イベント名が狭い画面で収まるようにする既存の文字サイズ調整を考慮してください。
- 開催中、準備中、交換期間、終了、更新日、最終日、終了間近の視覚的区別を維持してください。
- 日本語・英語・中国語でレイアウトを確認し、固定幅や日本語固有の長さを前提にしないでください。
- 表示文言を追加・変更するときは、原則として3言語すべての翻訳を同時に更新してください。翻訳キーがそのまま画面に表示されないことを確認してください。
- 設定画面のJSON編集、初期化、言語選択、ギルド向けコピー文の既存動作を壊さないでください。

# API / Data Rules

- `config.json`をイベント定義と翻訳の正本として扱ってください。
- `daily_events.json`は生成物です。手作業で内容だけを修正せず、原則として`config.json`または生成ロジックを修正して再生成してください。
- `daily_events.json`の`date`、`generatedAt`、`events`構造と既存フィールドの互換性を維持してください。
- 既存イベントIDは外部利用される可能性があるため、明示的な移行方針なしに変更・再利用・削除しないでください。
- 対応するイベント種別は`weekly_schedule`、`cycle_days`、`season_rounds`、`season_week_gates`、`remaining_only`、`weekly_update`です。
- 周期イベントの`startDate`は計算のアンカーです。根拠なく変更すると過去・将来の全日程がずれるため、ゲーム仕様またはユーザー指示を確認してください。
- `cycleDays`、`seasonDays`、`roundDays`、`updateWeekday`、`phases`、`gates`、`schedule`の意味と整合性を維持してください。
- ブラウザ固有のユーザー設定は`localStorage`に保存されます。既存キー`dada_info_config`と`dada_lang`を変更する場合は後方互換性を考慮してください。
- 現在データベースと独自APIはありません。必要性が明示されない限り追加しないでください。

# Security

- 秘密情報、APIキー、アクセストークン、パスワード、個人情報をソース、JSON、ログ、コミットへ保存しないでください。
- 秘密情報が必要になった場合はGitHub Secretsなど適切な秘密管理を使用し、公開されるGitHub Pagesのクライアントコードへ埋め込まないでください。
- 設定JSONと`localStorage`の内容は信頼しない入力として扱ってください。構造、型、日付、数値範囲、翻訳キーを検証してください。
- ユーザー編集可能な値を`innerHTML`へ直接挿入しないでください。DOM APIの`textContent`を優先し、HTMLが必要な場合は許可する構造を限定して安全に処理してください。
- 外部スクリプトや依存関係を追加するときは、必要性、供給元、バージョン固定、GitHub Pages上での影響を確認してください。
- GitHub Actionsの権限は必要最小限にし、`contents: write`を拡大しないでください。
- ログやエラー表示に設定内容、トークン、ローカルパスなどの機密情報を不用意に出力しないでください。

# Git Rules

- 作業開始時に`git status`を確認し、ユーザーの未コミット変更を上書き・破棄しないでください。
- Codexが新しいブランチを作る場合は、特に指定がなければ`codex/`接頭辞を使用してください。
- コミットは1つの目的に集中させ、変更理由が分かるメッセージにしてください。
- 自動生成コミットでは既存の`[skip ci]`運用を維持してください。
- `daily_events.json`はGitHub Actionsも更新します。機能変更に含める場合は、最新の生成日を古いデータで巻き戻さないよう差分と履歴を確認してください。
- `.github/workflows/daily_update.yml`を変更する場合は、JST午前1時以降に更新する目的、Node.js実行、書き込み権限、手動実行、差分がある場合だけコミットする挙動を維持してください。
- 強制push、履歴改変、破壊的なGit操作を、明示的な依頼なしに行わないでください。
- コード変更と無関係な生成ファイル、ログ、Antigravity由来の管理ファイルを同じ変更へ混ぜないでください。

# Do Not

- 既存機能を壊さないでください。
- 変更前の調査を省略しないでください。
- 不明点を推測してゲーム仕様、周期、起点日、表示要件を変更しないでください。
- 依頼されていない大規模リファクタリング、フレームワーク移行、ディレクトリ再編を行わないでください。
- `config.json`のイベントIDや翻訳キーを根拠なく変更しないでください。
- UTC+8基準をブラウザのローカル日付基準へ置き換えないでください。
- `daily_events.json`だけを手編集して永続的な修正としないでください。
- ブラウザ版とNode版のイベント計算を不一致にしないでください。
- ユーザー設定や翻訳値を未検証のままHTMLとして描画しないでください。
- GitHub Pagesでの静的配信を壊すサーバー依存や絶対パスを追加しないでください。
- 明示的な依頼なしに外部API、データベース、認証、トラッキングを追加しないでください。
- Antigravity固有の`.metadata.json`や`.resolved*`を新しい開発ルールの根拠として増やさないでください。
- ユーザーの許可なくファイル削除、生成物の大量更新、履歴変更を行わないでください。

# Verification

コードまたは設定を変更した後は、変更範囲に応じて以下を確認してください。

1. `git diff`で意図したファイルと内容だけが変更されている。
2. `node --check app.js`と`node --check export-events.js`が成功する。
3. `config.json`および生成した`daily_events.json`が有効なJSONである。
4. イベントIDが一意で、参照する翻訳キーが全言語に存在する。
5. UTC+8の日付境界、開始前、開始日、更新日、最終日、周期切替日の計算が正しい。
6. ブラウザ表示と`export-events.js`の出力が同じイベント状態を示す。
7. ローカルHTTPサーバーで初期表示、月移動、日付選択、今日ボタン、設定保存・初期化、言語切替、コピー機能が動作する。
8. デスクトップとモバイル幅でカレンダーとイベント一覧が崩れない。
9. 日本語・英語・中国語で翻訳キーの露出、欠落、レイアウト崩れがない。
10. ブラウザコンソールに新しいエラーがなく、`config.json`の取得に成功する。
11. `daily_events.json`を生成した場合、日付と差分が意図どおりで、最新データを過去の日付へ巻き戻していない。
12. GitHub Actionsを変更した場合、cronのUTC/JST換算、権限、生成、差分判定、pushの各処理を確認する。

すべての確認を実行できない場合は、未実施項目と理由、残るリスクを最終報告に明記してください。
